fun fib(n) {
  if (n < 2) {
    return n;
  }
  return fib(n - 2) + fib(n - 1);
}


print fib(10); // Output: 55

var start = clock();
print fib(35);
print "Time taken: " + (clock() - start) + " seconds.";
